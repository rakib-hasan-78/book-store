import React from 'react';
import Nav from '../Nav/Nav';

const Header = () => {
    return (
        <header className='w-full h-auto'>
            <Nav />
        </header>
    );
};

export default Header;